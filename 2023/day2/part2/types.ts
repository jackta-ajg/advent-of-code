export type GameInfo = {
  gameNumber: number;
  redCubes: Array<number>;
  greenCubes: Array<number>;
  blueCubes: Array<number>;
};

export type MaxCubesPerColor = {
  maxRedCubes: number;
  maxGreenCubes: number;
  maxBlueCubes: number;
};
